# 七国地图与艺术设定集

本次公开内容由用户授权接入多美小记与「故语·新说」。

- 阅读器：`/guyu/hanhai-realms-artbook`，52 页全幅 WebP，1536×2352，8 章、48 张场景图。封面、两页目录、48 张图版、封底均纳入连续页码。
- 地图入口：`/atlas-v6`，React 壳保留共享音乐；只嵌入同源 `/atlas/v6/index.html?embed=duomei`。独立静态页的顶层访问跳回壳，`?standalone=1` 仅供独立查看。
- 地图为 V6 卫星交互版：七个主要国家、六处中立政体、21 座城市、12 处要地、14 类地貌、24 条可点击交通路线、48 张关联风景图。地形与国界不在本次图集导入中重绘。
- 每幅图版附形成原因和景观结构。书页链接将实体 ID 作为 `entry` 参数交给地图，只接受地图已有国家、城市、要地 ID。
- 地图上的「设定集」链接保留真实 href。明确的同源多美 iframe 使用带源和窗口校验的消息交还 React 路由，避免重建播放器；其他页面保留普通跳转。
- 新书完全沿用其他新说册的既有 1.4 秒双页硬页翻书、封面展开与原生手势设置。已撤销单页模式及顶部地图/目录工具条，纸面目录仍保留；地图入口放入已有底部翻页栏。
- 公开书页聚合 SHA-256：`7af57eeca746e81dbe1f9784ed95338ee4ce62148fad5e497a46435fab1ac984`。
- 封面 SHA-256：`a538ceab803fad6d1403e82a79b72ebaf019f22daae155a809ec591fcfc463a3`。
- 地图原始场景均保留 1536×1024 画面尺寸，静态站使用质量 94 WebP。原版 PNG 属于七国图志源项目，不向故语鉴权或 R2 路径迁移。

`public/atlas/v6/` 是经核验的自包含静态导出，包括本地脚本、样式和图像；没有 CDN 运行依赖。其对应地图源项目为七国战略图志 V6，源代码在同一作者的独立地图项目维护。后续替换导出时须核对完整目录、实体定位、图片尺寸和子路径资源，不能只替换入口。

原 `map.duomei.site/v3/` 仍是原站点的既有版本；本次没有覆盖其独立部署，也没有把它冒充为 V6。当前新地图的多美正式入口是 `/atlas-v6`。
