Place the custom DUOMEI handwriting font here when it is ready.

The homepage intro already looks for:

- `duomei-handwriting.woff2`

Recommended export name:

- Font family: `Duomei Handwriting`

Fallback stack in use:

```css
"LXGW WenKai", "霞鹜文楷", "Ma Shan Zheng", "Zhi Mang Xing", "STKaiti", "KaiTi", serif
```
